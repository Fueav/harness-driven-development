{
  "schema_version": 1,
  "cases": [
    {"id": "bounded-correction", "scenario": "Fix a nil dereference in one internal parser without changing behavior or public contracts.", "expected_workflow": "HARNESS-FOCUSED-CHANGE", "expected_decision": "continue", "active_spec_required": false},
    {"id": "dev-cors-config", "scenario": "Add one owner-approved origin to an existing exact development CORS allowlist, deploy the development DockerHost service, and prove the header.", "expected_workflow": "HARNESS-FOCUSED-CHANGE", "expected_decision": "continue", "active_spec_required": false, "verification_contains": ["focused config contract", "full release only if explicitly required", "allowed and unlisted origins"]},
    {"id": "same-sha-evidence-reuse", "scenario": "A clean final commit already has passed release evidence. Fast-forward the deployment branch to that exact SHA and prepare deployment without changing semantics.", "expected_workflow": "HARNESS-FOCUSED-CHANGE", "expected_decision": "continue", "active_spec_required": false, "verification_contains": ["harnessctl evidence verify", "deployed revision"]},
    {"id": "api-semantic-change", "scenario": "Add a required field to a public API request and intentionally change validation semantics.", "expected_workflow": "HARNESS-SPEC-FIRST-FEATURE", "expected_decision": "continue", "active_spec_required": true},
    {"id": "read-only-incident", "scenario": "Investigate a production timeout from logs and traces; do not mutate files.", "expected_workflow": "HARNESS-VERIFICATION-INCIDENT", "expected_decision": "continue", "active_spec_required": false},
    {"id": "harness-maintenance", "scenario": "Remove duplicated Skill guidance and update Harness eval fixtures without product behavior changes.", "expected_workflow": "HARNESS-MAINTENANCE", "expected_decision": "continue", "active_spec_required": false},
    {"id": "ambiguous-money-semantics", "scenario": "Change settlement rounding, but two authoritative contracts disagree about half-cent behavior.", "expected_workflow": "HARNESS-SPEC-FIRST-FEATURE", "expected_decision": "stop", "active_spec_required": true, "reason_contains": "conflict"},
    {"id": "missing-approval", "scenario": "Deploy a bounded development configuration correction, but required deployment authority is unavailable.", "expected_workflow": "HARNESS-FOCUSED-CHANGE", "expected_decision": "stop", "active_spec_required": false, "reason_contains": "approval"},
    {"id": "failed-gate", "scenario": "Close out a bounded correction after its required repository release gate failed.", "expected_workflow": "HARNESS-FOCUSED-CHANGE", "expected_decision": "stop", "active_spec_required": false, "reason_contains": "gate"}
  ]
}
